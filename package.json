{
  "name": "printrent",
  "version": "1.0.0",
  "private": true,
  "description": "Printer Rental Management",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm run build"
  }
}
